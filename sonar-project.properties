##########################
# Required configuration #
##########################

sonar.projectKey=PROJECT_NAME
sonar.projectName=PROJECT_NAME
sonar.projectVersion=1.0
sonar.language=objc
sonar.objectivec.simulator=platform=iOS Simulator,name=iPhone 7,OS=10.0
 
# Project description
sonar.projectDescription=Fake description
 
# Path to source 
directories 
sonar.sources=PROJECT_NAME/Classes/
# Path to test directories (comment if no test)
sonar.tests=Example/Tests
sonar.host.url=http://sonar.baidao.com:9000/
 
# Xcode project configuration (.xcodeproj or .xcworkspace)
# -> If you have a project: configure only sonar.objectivec.project
# -> If you have a workspace: configure sonar.objectivec.workspace and sonar.objectivec.project
# and use the later to specify which project(s) to include in the analysis (comma separated list)
sonar.objectivec.project=Example/PROJECT_NAME.xcodeproj 
sonar.objectivec.workspace=Example/PROJECT_NAME.xcworkspace

# Scheme to build your application
sonar.objectivec.appScheme=PROJECT_NAME
# Scheme to build and run your tests (comment following line of you don't have any tests)
sonar.objectivec.testScheme=PROJECT_NAME
 
##########################
# Optional configuration #
##########################

# Encoding of the source code
sonar.sourceEncoding=UTF-8

# JUnit report generated by run-sonar.sh is stored in sonar-reports/TEST-report.xml
# Change it only if you generate the file on your own
# The XML files have to be prefixed by TEST- otherwise they are not processed 
# sonar.junit.reportsPath=sonar-reports/

# Cobertura report generated by run-sonar.sh is stored in sonar-reports/coverage.xml
# Change it only if you generate the file on your own
sonar.objectivec.coverage.reportPattern=coverage_report/*.xml

# OCLint report generated by run-sonar.sh is stored in sonar-reports/oclint.xml
# Change it only if you generate the file on your own
# sonar.objectivec.oclint.report=sonar-reports/oclint.xml

# Paths to exclude from coverage report (tests, 3rd party libraries etc.)
# sonar.objectivec.excludedPathsFromCoverage=pattern1,pattern2
sonar.objectivec.excludedPathsFromCoverage=.*Tests.*

# Project SCM settings
# sonar.scm.enabled=true
# sonar.scm.url=scm:git:https://...
